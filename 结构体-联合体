
#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
//联合-联合体-共用体
union un
{
	char a;
	int i;
};
//联合体的成员共用同一块内存空间，这样一个联合变量的大小至少是最大成员的大小
int main()
{
	union un m;
	printf("%d\n", sizeof(m));
	printf("%p\n", &m);
	printf("%p\n", &m.a);
	printf("%p\n", &m.i);
	//三个量地址相同
	return 0;
}
